/// Central registry of asset paths (agent app).
///
/// Abhi koi bundled data nahi (orders runtime pe mock/Firebase se aayenge).
/// Images/logos add karte waqt sirf yahan path likhna.
class AppAssets {
  AppAssets._();
  // static const String logo = 'assets/images/logo.png';
}
