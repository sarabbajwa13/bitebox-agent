rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // DEV ONLY — abhi testing ke liye open. Production se pehle tighten karna
    // (auth-based: sirf apne store ke orders, etc.).
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
